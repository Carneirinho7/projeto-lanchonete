# Projeto Lanchonete

Estrutura do repositório:

- `frontend/`: aplicação React/Vite.
- `backend/`: espaço reservado para a API e serviços.

Para executar o frontend:

```bash
cd frontend
npm run dev
```
